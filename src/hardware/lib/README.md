# RYHand vendor library

Copy the vendor-provided library into this directory as `libRyhand64.so`.
Vendors may distribute it as `libRyhand64.so.1`; the runtime filename is still
`libRyhand64.so`. The binary is intentionally ignored by Git and required for
live hand control.
